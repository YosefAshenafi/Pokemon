/**
 * Stub for `import '../global.css'`. Jest has no CSS loader; NativeWind's
 * classes are resolved by the babel transform, so the stylesheet itself has
 * nothing to contribute at test time.
 */
module.exports = {};
